<%- include('../partials/header') %>

<main class="maintenance-form-page">
    <div class="form-card">
        <header class="form-header">
            <h1>New Maintenance Request</h1>
            <p>Please describe the issue in detail. We will notify you once a manager is assigned.</p>
        </header>

        <% if (locals.messages && locals.messages.error) { %>
            <div class="alert alert-error" style="color: red; margin-bottom: 1rem;">
                <%= messages.error %>
            </div>
        <% } %>

        <form action="/maintenance/new" method="POST" class="styled-form">
            <div class="form-group">
                <label for="title">Issue Summary</label>
                <input type="text" id="title" name="title" placeholder="e.g., HVAC not cooling" required>
            </div>

            <div class="form-row">
                <div class="form-group">
                    <label for="unitId">Unit</label>
                    <select id="unitId" name="unitId" required>
                        <option value="" disabled selected>Select your unit...</option>
                        <% if (units && units.length > 0) { %>
                            <% units.forEach(unit => { %>
                                <option value="<%= unit.id %>">
                                    <%= unit.property_name %> - Unit <%= unit.unit_number %>
                                </option>
                            <% }) %>
                        <% } else { %>
                            <option value="" disabled>No units available</option>
                        <% } %>
                    </select>
                </div>

                <div class="form-group">
                    <label for="priority">Priority</label>
                    <select id="priority" name="priority">
                        <option value="low">Low</option>
                        <option value="medium" selected>Medium</option>
                        <option value="high">High</option>
                        <option value="emergency">Emergency</option>
                    </select>
                </div>
            </div>

            <div class="form-group">
                <label for="description">Detailed Description</label>
                <textarea id="description" name="description" rows="5" placeholder="Explain the location and nature of the problem..." required></textarea>
            </div>

            <div class="form-actions">
                <button type="submit" class="btn-primary-sm">Submit Request</button>
                <a href="/maintenance" class="btn-secondary">Cancel</a>
            </div>
        </form>
    </div>
</main>

<%- include('../partials/footer') %>